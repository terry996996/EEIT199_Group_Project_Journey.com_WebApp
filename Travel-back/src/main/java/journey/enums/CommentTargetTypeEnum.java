package journey.enums;

public enum CommentTargetTypeEnum {
    LODGING, // 對應 lodgingId
    ROOM_TYPE, // 對應 roomTypeId
    // TRAFFIC_TICKET, // 對應 trafficTicketId
    // ATTRACTION_TICKET, // 對應 attractionTicketId
    // TRIP_PLAN // 對應 tripPlanId
}